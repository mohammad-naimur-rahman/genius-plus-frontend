const style = ['realistic', 'abstract', 'cartoonish', 'sketch', 'digital art', '3D render']

const color_scheme = ['vibrant', 'pastel', 'monochrome', 'neon', 'sepia']

const perspective = ['wide-angle', 'close-up', "bird's-eye view", 'fisheye', 'panoramic']

const lighting = ['natural', 'dramatic', 'soft', 'high contrast', 'studio lighting']

const mood = ['calm', 'mysterious', 'joyful', 'dark', 'whimsical']

const background_type = ['solid color', 'gradient', 'scenery', 'patterned', 'transparent']

const texture = ['smooth', 'rough', 'grainy', 'metallic', 'wooden']

const environment = ['urban', 'rural', 'forest', 'desert', 'underwater', 'outer space']

const movement = ['still', 'in motion', 'blurred motion', 'slow-motion']

const style_intensity = ['subtle', 'moderate', 'strong', 'over-the-top']

export const imgGenOptions = {
  style,
  color_scheme,
  perspective,
  lighting,
  mood,
  background_type,
  texture,
  environment,
  movement,
  style_intensity
}

export const imageAspects = ['square', 'portrait', 'landscape']
export const imageStyles = ['natural', 'vivid']
